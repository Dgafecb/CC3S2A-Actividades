version: '3.8'

services:
  app:
    build: Actividad1-Introduccion/devops-practice
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    container_name: devops-practice-container
